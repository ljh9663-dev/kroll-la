{
  "name": "kroll-la",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev . --port 3000",
    "deploy": "npx wrangler pages deploy . --project-name kroll-la"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
